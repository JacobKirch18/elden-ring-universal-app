<Page
    x:Class="eldenRingUniversalApp.CompendiumPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:eldenRingUniversalApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="Dark">

    <RelativePanel>
        <CommandBar x:Name="commandBar" RelativePanel.AlignRightWithPanel="True">
            <AppBarButton Icon="Home" Label="Compendium" Click="homeButton_Click" Width="85" />
            <AppBarButton Icon="Help" Label="About" Click="aboutGameButton_Click" Width="85" />
            <AppBarButton Icon="Find" Label="Search" Click="searchButton_Click" Width="85" />
        </CommandBar>
        <TextBox x:Name="searchTextBox" Text="Type to search..." TextWrapping="Wrap" Height="35" Width="176" 
           RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="commandBar" Margin="0,20,50,20" Visibility="Collapsed"
           GotFocus="searchTextBox_GotFocus" LostFocus="searchTextBox_LostFocus" 
           TextChanged="searchTextBox_TextChanged" />
        <TextBlock x:Name="compendiumText" Text="Compendium" FontSize="50" Padding="50" RelativePanel.AlignHorizontalCenterWithPanel="True"/>
        <TextBlock x:Name="levelText" Text="New Game+ Level" FontSize="20" Padding ="20" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="compendiumText"/>
        <StackPanel x:Name="radioButtonsStackPanel" Orientation="Horizontal" Padding="10" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="levelText">
            <RadioButton x:Name="rb0" Content="NG+ 0" IsChecked="True"/>
            <RadioButton x:Name="rb1" Content="NG+ 1"/>
            <RadioButton x:Name="rb2" Content="NG+ 2"/>
            <RadioButton x:Name="rb3" Content="NG+ 3"/>
            <RadioButton x:Name="rb4" Content="NG+ 4"/>
            <RadioButton x:Name="rb5" Content="NG+ 5"/>
            <RadioButton x:Name="rb6" Content="NG+ 6"/>
            <RadioButton x:Name="rb7" Content="NG+ 7"/>
        </StackPanel>
        <Line x:Name="divLine" X1="0" Y1="0" X2="{Binding ElementName=radioButtonsStackPanel, Path=ActualWidth}" Y2="1" 
              Stroke="White" StrokeThickness="2" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="radioButtonsStackPanel" />
        <ListView x:Name="bossListView" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="divLine" ItemsSource="{x:Bind defeatedBosses}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Boss">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name}" FontWeight="Bold" />
                        <Image Source="{x:Bind Image}" />
                        <TextBlock Text="{x:Bind Description}" />
                        <TextBlock Text="{x:Bind Location}" />
                        <ListView x:Name="dropsListView" ItemsSource="{x:Bind Drops}">
                            <!-- Asked ChatGPT "How to make the list boxes not clickable and not highlight on mouse hover -->
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Background" Value="Transparent"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontWeight="Bold" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBlock Text="{x:Bind HealthPoints}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </RelativePanel>
</Page>
